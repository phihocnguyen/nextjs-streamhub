import SettingForm from "@/app/(protected)/settings/setting-form"

const Settings = async () => {

  return (
    <div className="flex flex-col items-center justify-center h-screen">
        <SettingForm/>
    </div>
  )
}

export default Settings